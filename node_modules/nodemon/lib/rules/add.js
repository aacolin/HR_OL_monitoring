'use strict';

var utils = require('../utils');

// internal
var reEscComments = /\\#/g;
// note that '^^' is used in place of escaped comments
var reUnescapeComments = /\^\^/g;
var reComments = /#.*$/;
var reEscapeChars = /[.|\-[\]()\\]/g;
var reAsterisk = /\*/g;

module.e***REMOVED***ports = add;

/**
 * Converts file patterns or regular e***REMOVED***pressions to nodemon
 * compatible RegE***REMOVED***p matching rules. Note: the `rules` argument
 * object is modified to include the new rule and new RegE***REMOVED***p
 *
 * ### E***REMOVED***ample:
 *
 *     var rules = { watch: [], ignore: [] };
 *     add(rules, 'watch', '*.js');
 *     add(rules, 'ignore', '/public/');
 *     add(rules, 'watch', ':(\d)*\.js'); // note: string based rege***REMOVED***p
 *     add(rules, 'watch', /\d*\.js/);
 *
 * @param {Object} rules containing `watch` and `ignore`. Also updated during
 *                       e***REMOVED***ecution
 * @param {String} which must be either "watch" or "ignore"
 * @param {String|RegE***REMOVED***p} the actual rule.
 */
function add(rules, which, rule) {
  if (!{ ignore: 1, watch: 1}[which]) {
    throw new Error('rules/inde***REMOVED***.js#add requires "ignore" or "watch" as the ' +
      'first argument');
  }

  if (Array.isArray(rule)) {
    rule.forEach(function (rule) {
      add(rules, which, rule);
    });
    return;
  }

  // support the rule being a RegE***REMOVED***p, but reformat it to
  // the custom :<rege***REMOVED***p> format that we're working with.
  if (rule instanceof RegE***REMOVED***p) {
    // rule = ':' + rule.toString().replace(/^\/(.*?)\/$/g, '$1');
    utils.log.error('RegE***REMOVED***p format no longer supported, but globs are.');
    return;
  }

  // remove comments and trim lines
  // this mess of replace methods is escaping "\#" to allow for emacs temp files

  // first up strip comments and remove blank head or tails
  rule = (rule || '').replace(reEscComments, '^^')
             .replace(reComments, '')
             .replace(reUnescapeComments, '#').trim();

  var rege***REMOVED***p = false;

  if (typeof rule === 'string' && rule.substring(0, 1) === ':') {
    rule = rule.substring(1);
    utils.log.error('RegE***REMOVED***p no longer supported: ' + rule);
    rege***REMOVED***p = true;
  } else if (rule.length === 0) {
    // blank line (or it was a comment)
    return;
  }

  if (rege***REMOVED***p) {
    // rules[which].push(rule);
  } else {
    // rule = rule.replace(reEscapeChars, '\\$&')
    // .replace(reAsterisk, '.*');

    rules[which].push(rule);
    // compile a rege***REMOVED***p of all the rules for this ignore or watch
    var re = rules[which].map(function (rule) {
      return rule.replace(reEscapeChars, '\\$&')
                 .replace(reAsterisk, '.*');
    }).join('|');

    // used for the directory matching
    rules[which].re = new RegE***REMOVED***p(re);
  }
}
