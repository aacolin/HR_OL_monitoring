var fs = require('fs');
var path = require('path');
const supportsColor = require('supports-color');

module.e***REMOVED***ports = help;

const highlight = supportsColor.stdout ? '\***REMOVED***1B\[$1m' : '';

function help(item) {
  if (!item) {
    item = 'help';
  } else if (item === true) { // if used with -h or --help and no args
    item = 'help';
  }

  // cleanse the filename to only contain letters
  // aka: /\W/g but figured this was eaiser to read
  item = item.replace(/[^a-z]/gi, '');

  try {
    var dir = path.join(__dirname, '..', '..', 'doc', 'cli', item + '.t***REMOVED***t');
    var body = fs.readFileSync(dir, 'utf8');
    return body.replace(/\\***REMOVED***1B\[(.)m/g, highlight);
  } catch (e) {
    return '"' + item + '" help can\'t be found';
  }
}
