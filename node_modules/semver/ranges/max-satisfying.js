const SemVer = require('../classes/semver')
const Range = require('../classes/range')

const ma***REMOVED***Satisfying = (versions, range, options) => {
  let ma***REMOVED*** = null
  let ma***REMOVED***SV = null
  let rangeObj = null
  try {
    rangeObj = new Range(range, options)
  } catch (er) {
    return null
  }
  versions.forEach((v) => {
    if (rangeObj.test(v)) {
      // satisfies(v, range, options)
      if (!ma***REMOVED*** || ma***REMOVED***SV.compare(v) === -1) {
        // compare(ma***REMOVED***, v, true)
        ma***REMOVED*** = v
        ma***REMOVED***SV = new SemVer(ma***REMOVED***, options)
      }
    }
  })
  return ma***REMOVED***
}
module.e***REMOVED***ports = ma***REMOVED***Satisfying
