"use strict";
Object.defineProperty(e***REMOVED***ports, "__esModule", { value: true });
e***REMOVED***ports.tokenCache = e***REMOVED***ports.AzureCredentialCache = e***REMOVED***ports.AZURE_BASE_URL = void 0;
e***REMOVED***ports.addAzureParams = addAzureParams;
e***REMOVED***ports.prepareRequest = prepareRequest;
e***REMOVED***ports.fetchAzureKMSToken = fetchAzureKMSToken;
e***REMOVED***ports.loadAzureCredentials = loadAzureCredentials;
const error_1 = require("../../error");
const utils_1 = require("../../utils");
const errors_1 = require("../errors");
const MINIMUM_TOKEN_REFRESH_IN_MILLISECONDS = 6000;
/** Base URL for getting Azure tokens. */
e***REMOVED***ports.AZURE_BASE_URL = 'http://169.254.169.254/metadata/identity/oauth2/token?';
/**
 * @internal
 */
class AzureCredentialCache {
    constructor() {
        this.cachedToken = null;
    }
    async getToken() {
        if (this.cachedToken == null || this.needsRefresh(this.cachedToken)) {
            this.cachedToken = await this._getToken();
        }
        return { accessToken: this.cachedToken.accessToken };
    }
    needsRefresh(token) {
        const timeUntilE***REMOVED***pirationMS = token.e***REMOVED***piresOnTimestamp - Date.now();
        return timeUntilE***REMOVED***pirationMS <= MINIMUM_TOKEN_REFRESH_IN_MILLISECONDS;
    }
    /**
     * e***REMOVED***posed for testing
     */
    resetCache() {
        this.cachedToken = null;
    }
    /**
     * e***REMOVED***posed for testing
     */
    _getToken() {
        return fetchAzureKMSToken();
    }
}
e***REMOVED***ports.AzureCredentialCache = AzureCredentialCache;
/** @internal */
e***REMOVED***ports.tokenCache = new AzureCredentialCache();
/** @internal */
async function parseResponse(response) {
    const { status, body: rawBody } = response;
    const body = (() => {
        try {
            return JSON.parse(rawBody);
        }
        catch {
            throw new errors_1.MongoCryptAzureKMSRequestError('Malformed JSON body in GET request.');
        }
    })();
    if (status !== 200) {
        throw new errors_1.MongoCryptAzureKMSRequestError('Unable to complete request.', body);
    }
    if (!body.access_token) {
        throw new errors_1.MongoCryptAzureKMSRequestError('Malformed response body - missing field `access_token`.');
    }
    if (!body.e***REMOVED***pires_in) {
        throw new errors_1.MongoCryptAzureKMSRequestError('Malformed response body - missing field `e***REMOVED***pires_in`.');
    }
    const e***REMOVED***piresInMS = Number(body.e***REMOVED***pires_in) * 1000;
    if (Number.isNaN(e***REMOVED***piresInMS)) {
        throw new errors_1.MongoCryptAzureKMSRequestError('Malformed response body - unable to parse int from `e***REMOVED***pires_in` field.');
    }
    return {
        accessToken: body.access_token,
        e***REMOVED***piresOnTimestamp: Date.now() + e***REMOVED***piresInMS
    };
}
/**
 * @internal
 * Get the Azure endpoint URL.
 */
function addAzureParams(url, resource, username) {
    url.searchParams.append('api-version', '2018-02-01');
    url.searchParams.append('resource', resource);
    if (username) {
        url.searchParams.append('client_id', username);
    }
    return url;
}
/**
 * @internal
 *
 * parses any options provided by prose tests to `fetchAzureKMSToken` and merges them with
 * the default values for headers and the request url.
 */
function prepareRequest(options) {
    const url = new URL(options.url?.toString() ?? e***REMOVED***ports.AZURE_BASE_URL);
    addAzureParams(url, 'https://vault.azure.net');
    const headers = { ...options.headers, 'Content-Type': 'application/json', Metadata: true };
    return { headers, url };
}
/**
 * @internal
 *
 * `AzureKMSRequestOptions` allows prose tests to modify the http request sent to the idms
 * servers.  This is required to simulate different server conditions.  No options are e***REMOVED***pected to
 * be set outside of tests.
 *
 * e***REMOVED***posed for CSFLE
 * [prose test 18](https://github.com/mongodb/specifications/tree/master/source/client-side-encryption/tests#azure-imds-credentials)
 */
async function fetchAzureKMSToken(options = {}) {
    const { headers, url } = prepareRequest(options);
    try {
        const response = await (0, utils_1.get)(url, { headers });
        return await parseResponse(response);
    }
    catch (error) {
        if (error instanceof error_1.MongoNetworkTimeoutError) {
            throw new errors_1.MongoCryptAzureKMSRequestError(`[Azure KMS] ${error.message}`);
        }
        throw error;
    }
}
/**
 * @internal
 *
 * @throws Will reject with a `MongoCryptError` if the http request fails or the http response is malformed.
 */
async function loadAzureCredentials(kmsProviders) {
    const azure = await e***REMOVED***ports.tokenCache.getToken();
    return { ...kmsProviders, azure };
}
//# sourceMappingURL=azure.js.map