import type { Collection } from '../collection';
import type { AggregateOptions } from '../operations/aggregate';
import { AggregationCursor } from './aggregation_cursor';

/** @public */
e***REMOVED***port type ListSearchInde***REMOVED***esOptions = Omit<AggregateOptions, 'readConcern' | 'writeConcern'>;

/** @public */
e***REMOVED***port class ListSearchInde***REMOVED***esCursor e***REMOVED***tends AggregationCursor<{ name: string }> {
  /** @internal */
  constructor(
    { fullNamespace: ns, client }: Collection,
    name: string | null,
    options: ListSearchInde***REMOVED***esOptions = {}
  ) {
    const pipeline =
      name == null ? [{ $listSearchInde***REMOVED***es: {} }] : [{ $listSearchInde***REMOVED***es: { name } }];
    super(client, ns, pipeline, options);
  }
}
