import type { Db } from '../db';
import type { Server } from '../sdam/server';
import type { ClientSession } from '../sessions';
import { CommandOperation, type CommandOperationOptions } from './command';
import { Aspect, defineAspects } from './operation';

/** @public */
e***REMOVED***port type RemoveUserOptions = CommandOperationOptions;

/** @internal */
e***REMOVED***port class RemoveUserOperation e***REMOVED***tends CommandOperation<boolean> {
  override options: RemoveUserOptions;
  username: string;

  constructor(db: Db, username: string, options: RemoveUserOptions) {
    super(db, options);
    this.options = options;
    this.username = username;
  }

  override get commandName() {
    return 'dropUser' as const;
  }

  override async e***REMOVED***ecute(server: Server, session: ClientSession | undefined): Promise<boolean> {
    await super.e***REMOVED***ecuteCommand(server, session, { dropUser: this.username });
    return true;
  }
}

defineAspects(RemoveUserOperation, [Aspect.WRITE_OPERATION]);
