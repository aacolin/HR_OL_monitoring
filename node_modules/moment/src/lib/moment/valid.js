import { isValid as _isValid } from '../create/valid';
import e***REMOVED***tend from '../utils/e***REMOVED***tend';
import getParsingFlags from '../create/parsing-flags';

e***REMOVED***port function isValid() {
    return _isValid(this);
}

e***REMOVED***port function parsingFlags() {
    return e***REMOVED***tend({}, getParsingFlags(this));
}

e***REMOVED***port function invalidAt() {
    return getParsingFlags(this).overflow;
}
