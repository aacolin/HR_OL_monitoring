e***REMOVED***port function valueOf() {
    return this._d.valueOf() - (this._offset || 0) * 60000;
}

e***REMOVED***port function uni***REMOVED***() {
    return Math.floor(this.valueOf() / 1000);
}

e***REMOVED***port function toDate() {
    return new Date(this.valueOf());
}

e***REMOVED***port function toArray() {
    var m = this;
    return [
        m.year(),
        m.month(),
        m.date(),
        m.hour(),
        m.minute(),
        m.second(),
        m.millisecond(),
    ];
}

e***REMOVED***port function toObject() {
    var m = this;
    return {
        years: m.year(),
        months: m.month(),
        date: m.date(),
        hours: m.hours(),
        minutes: m.minutes(),
        seconds: m.seconds(),
        milliseconds: m.milliseconds(),
    };
}

e***REMOVED***port function toJSON() {
    // new Date(NaN).toJSON() === null
    return this.isValid() ? this.toISOString() : null;
}
