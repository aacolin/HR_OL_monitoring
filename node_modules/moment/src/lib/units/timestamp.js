import { addFormatToken } from '../format/format';
import { addRege***REMOVED***Token, matchTimestamp, matchSigned } from '../parse/rege***REMOVED***';
import { addParseToken } from '../parse/token';
import toInt from '../utils/to-int';

// FORMATTING

addFormatToken('X', 0, 0, 'uni***REMOVED***');
addFormatToken('***REMOVED***', 0, 0, 'valueOf');

// PARSING

addRege***REMOVED***Token('***REMOVED***', matchSigned);
addRege***REMOVED***Token('X', matchTimestamp);
addParseToken('X', function (input, array, config) {
    config._d = new Date(parseFloat(input) * 1000);
});
addParseToken('***REMOVED***', function (input, array, config) {
    config._d = new Date(toInt(input));
});
