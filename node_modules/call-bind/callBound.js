'use strict';

var GetIntrinsic = require('get-intrinsic');

var callBind = require('./');

var $inde***REMOVED***Of = callBind(GetIntrinsic('String.prototype.inde***REMOVED***Of'));

module.e***REMOVED***ports = function callBoundIntrinsic(name, allowMissing) {
	var intrinsic = GetIntrinsic(name, !!allowMissing);
	if (typeof intrinsic === 'function' && $inde***REMOVED***Of(name, '.prototype.') > -1) {
		return callBind(intrinsic);
	}
	return intrinsic;
};
