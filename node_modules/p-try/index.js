'use strict';

const pTry = (fn, ...arguments_) => new Promise(resolve => {
	resolve(fn(...arguments_));
});

module.e***REMOVED***ports = pTry;
// TODO: remove this in the ne***REMOVED***t major version
module.e***REMOVED***ports.default = pTry;
