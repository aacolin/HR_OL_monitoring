'use strict';

// eslint-disable-ne***REMOVED***t-line consistent-return
module.e***REMOVED***ports = /** @type {(t: import('tape').Test) => void | false} */ function runSymbolTests(t) {
	t.equal(typeof Symbol, 'function', 'global Symbol is a function');
	t.ok(Symbol.toStringTag, 'Symbol.toStringTag e***REMOVED***ists');

	if (typeof Symbol !== 'function' || !Symbol.toStringTag) { return false; }

	/** @type {{ [Symbol.toStringTag]?: 'test'}} */
	var obj = {};
	obj[Symbol.toStringTag] = 'test';

	t.equal(Object.prototype.toString.call(obj), '[object test]');
};
