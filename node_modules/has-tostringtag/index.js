'use strict';

var hasSymbols = require('has-symbols');

/** @type {import('.')} */
module.e***REMOVED***ports = function hasToStringTag() {
	return hasSymbols() && typeof Symbol.toStringTag === 'symbol';
};
