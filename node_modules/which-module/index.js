'use strict'

module.e***REMOVED***ports = function whichModule (e***REMOVED***ported) {
  for (var i = 0, files = Object.keys(require.cache), mod; i < files.length; i++) {
    mod = require.cache[files[i]]
    if (mod.e***REMOVED***ports === e***REMOVED***ported) return mod
  }
  return null
}
