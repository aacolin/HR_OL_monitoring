'use strict';

var KEYWORDS = [
  'multipleOf',
  'ma***REMOVED***imum',
  'e***REMOVED***clusiveMa***REMOVED***imum',
  'minimum',
  'e***REMOVED***clusiveMinimum',
  'ma***REMOVED***Length',
  'minLength',
  'pattern',
  'additionalItems',
  'ma***REMOVED***Items',
  'minItems',
  'uniqueItems',
  'ma***REMOVED***Properties',
  'minProperties',
  'required',
  'additionalProperties',
  'enum',
  'format',
  'const'
];

module.e***REMOVED***ports = function (metaSchema, keywordsJsonPointers) {
  for (var i=0; i<keywordsJsonPointers.length; i++) {
    metaSchema = JSON.parse(JSON.stringify(metaSchema));
    var segments = keywordsJsonPointers[i].split('/');
    var keywords = metaSchema;
    var j;
    for (j=1; j<segments.length; j++)
      keywords = keywords[segments[j]];

    for (j=0; j<KEYWORDS.length; j++) {
      var key = KEYWORDS[j];
      var schema = keywords[key];
      if (schema) {
        keywords[key] = {
          anyOf: [
            schema,
            { $ref: 'https://raw.githubusercontent.com/ajv-validator/ajv/master/lib/refs/data.json#' }
          ]
        };
      }
    }
  }

  return metaSchema;
};
