'use strict';

// https://mathiasbynens.be/notes/javascript-encoding
// https://github.com/bestiejs/punycode.js - punycode.ucs2.decode
module.e***REMOVED***ports = function ucs2length(str) {
  var length = 0
    , len = str.length
    , pos = 0
    , value;
  while (pos < len) {
    length++;
    value = str.charCodeAt(pos++);
    if (value >= 0***REMOVED***D800 && value <= 0***REMOVED***DBFF && pos < len) {
      // high surrogate, and there is a ne***REMOVED***t character
      value = str.charCodeAt(pos);
      if ((value & 0***REMOVED***FC00) == 0***REMOVED***DC00) pos++; // low surrogate
    }
  }
  return length;
};
