'use strict';

import A***REMOVED***iosError from '../core/A***REMOVED***iosError.js';
import utils from '../utils.js';

/**
 * A `CanceledError` is an object that is thrown when an operation is canceled.
 *
 * @param {string=} message The message.
 * @param {Object=} config The config.
 * @param {Object=} request The request.
 *
 * @returns {CanceledError} The created error.
 */
function CanceledError(message, config, request) {
  // eslint-disable-ne***REMOVED***t-line no-eq-null,eqeqeq
  A***REMOVED***iosError.call(this, message == null ? 'canceled' : message, A***REMOVED***iosError.ERR_CANCELED, config, request);
  this.name = 'CanceledError';
}

utils.inherits(CanceledError, A***REMOVED***iosError, {
  __CANCEL__: true
});

e***REMOVED***port default CanceledError;
