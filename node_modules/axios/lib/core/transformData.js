'use strict';

import utils from './../utils.js';
import defaults from '../defaults/inde***REMOVED***.js';
import A***REMOVED***iosHeaders from '../core/A***REMOVED***iosHeaders.js';

/**
 * Transform the data for a request or a response
 *
 * @param {Array|Function} fns A single function or Array of functions
 * @param {?Object} response The response object
 *
 * @returns {*} The resulting transformed data
 */
e***REMOVED***port default function transformData(fns, response) {
  const config = this || defaults;
  const conte***REMOVED***t = response || config;
  const headers = A***REMOVED***iosHeaders.from(conte***REMOVED***t.headers);
  let data = conte***REMOVED***t.data;

  utils.forEach(fns, function transform(fn) {
    data = fn.call(config, data, headers.normalize(), response ? response.status : undefined);
  });

  headers.normalize();

  return data;
}
