'use strict';

import utils from './../utils.js';

/**
 * Determines whether the payload is an error thrown by A***REMOVED***ios
 *
 * @param {*} payload The value to test
 *
 * @returns {boolean} True if the payload is an error thrown by A***REMOVED***ios, otherwise false
 */
e***REMOVED***port default function isA***REMOVED***iosError(payload) {
  return utils.isObject(payload) && (payload.isA***REMOVED***iosError === true);
}
