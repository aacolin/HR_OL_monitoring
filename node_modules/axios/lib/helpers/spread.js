'use strict';

/**
 * Syntactic sugar for invoking a function and e***REMOVED***panding an array for arguments.
 *
 * Common use case would be to use `Function.prototype.apply`.
 *
 *  ```js
 *  function f(***REMOVED***, y, z) {}
 *  var args = [1, 2, 3];
 *  f.apply(null, args);
 *  ```
 *
 * With `spread` this e***REMOVED***ample can be re-written.
 *
 *  ```js
 *  spread(function(***REMOVED***, y, z) {})([1, 2, 3]);
 *  ```
 *
 * @param {Function} callback
 *
 * @returns {Function}
 */
e***REMOVED***port default function spread(callback) {
  return function wrap(arr) {
    return callback.apply(null, arr);
  };
}
