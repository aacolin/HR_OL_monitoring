import { e***REMOVED***pect, sinon } from './test-setup';
import Client from '../src/Client';
import * as fi***REMOVED***tures from './fi***REMOVED***tures';
import Library from '../src/Library';

let api;
const token = 'tok';
let client;


describe('Client', () => {
	beforeEach(() => {
		api = {};
		client = new Client({ api: api, auth: token });
	});

	describe('constructor', () => {
		it('sets the auth token', () => {
			e***REMOVED***pect(client.auth).to.equal(token);
		});
		it('sets the api', () => {
			e***REMOVED***pect(client.api).to.equal(api);
		});
	});

	describe('libraries', () => {
		it('resolves to a list of Library objects', () => {
			api.listLibraries = () => Promise.resolve({ body: fi***REMOVED***tures.readJSON('libraries.json') });
			return client.libraries().then(libraries => {
				e***REMOVED***pect(libraries.length).to.equal(1);
				e***REMOVED***pect(libraries[0].name).to.equal('neopi***REMOVED***el');
			});
		});
	});

	describe('library', () => {
		it('resolves to a Library objects', () => {
			api.getLibrary = () => Promise.resolve({ body: fi***REMOVED***tures.readJSON('library.json') });
			return client.library('neopi***REMOVED***el').then(library => {
				e***REMOVED***pect(library.name).to.equal('neopi***REMOVED***el');
			});
		});
	});

	describe('libraryVersions', () => {
		it('resolves to a Library objects', () => {
			api.getLibraryVersions = () => Promise.resolve({ body: fi***REMOVED***tures.readJSON('libraryVersions.json') });
			return client.libraryVersions().then(libraries => {
				e***REMOVED***pect(libraries.length).to.equal(9);
				e***REMOVED***pect(libraries[0].name).to.equal('neopi***REMOVED***el');
				e***REMOVED***pect(libraries[0].version).to.equal('0.0.10');
				e***REMOVED***pect(libraries[1].version).to.equal('0.0.9');
			});
		});
	});

	describe('downloadFile', () => {
		it('delegates to api', () => {
			api.downloadFile = ({ uri }) => Promise.resolve(`${uri} delegated`);
			return e***REMOVED***pect(client.downloadFile('uri')).to.eventually.equal('uri delegated');
		});
	});

	describe('compileCode', () => {
		it('delegates to api', () => {
			api.compileCode = ({ files, platformId, targetVersion, auth }) => {
				return Promise.resolve([files, platformId, targetVersion, auth]);
			};
			return e***REMOVED***pect(client.compileCode('a', 'b', 'c')).to.eventually.eql(['a', 'b', 'c', client.auth]);
		});
	});

	describe('signalDevice', () => {
		it('delegates to api', () => {
			api.signalDevice = () => {
				return Promise.resolve([true, client.auth]);
			};
			return e***REMOVED***pect(client.signalDevice({ deviceId: 'testid', signal: true }))
				.to.eventually.eql([true, client.auth]);
		});
	});

	describe('publishLibrary', () => {
		it('delegates to api and returns the library metadata on success', () => {
			const name = 'fred';
			const metadata = { name };
			const library = new Library(client, metadata);
			api.publishLibrary = sinon.stub().resolves({ body: { data: metadata } });
			return client.publishLibrary(name)
				.then(actual => {
					e***REMOVED***pect(actual).to.eql(library);
					e***REMOVED***pect(api.publishLibrary).to.have.been.calledWith({ name, auth:token });
				});
		});

		it('delegates to api and calls _throwError to handle the error', () => {
			const error = { message:'I don\'t like vegetables' };
			api.publishLibrary = sinon.stub().rejects(error);
			const name = 'notused';
			return client.publishLibrary(name)
				.then(() => {
					throw new Error('e***REMOVED***pected an e***REMOVED***ception');
				})
				.catch(actual => {
					e***REMOVED***pect(actual).to.eql(error);
					e***REMOVED***pect(api.publishLibrary).to.have.been.calledWith({ name, auth:token });
				});
		});
	});

	describe('contributeLibrary', () => {
		it('delegates to api and returns the library metadata on success', () => {
			const archive = {};
			const metadata = { name:'' };
			const library = new Library(client, metadata);
			api.contributeLibrary = sinon.stub().resolves({ body: { data: metadata } });
			return client.contributeLibrary(archive)
				.then(actual => {
					e***REMOVED***pect(actual).to.eql(library);
					e***REMOVED***pect(api.contributeLibrary).to.have.been.calledWith({ archive, auth:token });
				});
		});

		it('delegates to api and calls _throwError to handle the error', () => {
			const archive = {};
			const error = { message:'I don\'t like vegetables' };
			api.contributeLibrary = sinon.stub().rejects(error);
			return client.contributeLibrary(archive)
				.then(() => {
					throw new Error('e***REMOVED***pected an e***REMOVED***ception');
				})
				.catch(actual => {
					e***REMOVED***pect(actual).to.eql(error);
					e***REMOVED***pect(api.contributeLibrary).to.have.been.calledWith({ archive, auth:token });
				});
		});
	});

	describe('listDevices', () => {
		it('delegates to api', () => {
			api.listDevices = ({ auth }) => {
				return Promise.resolve([auth]);
			};
			return e***REMOVED***pect(client.listDevices()).to.eventually.eql([client.auth]);
		});
	});

	describe('listBuildTargets', () => {
		it('delegates to api', () => {
			const response = {
				targets: [
					{
						platforms: [0, 6],
						prereleases: [],
						version: '1.2.3',
						firmware_vendor: 'Foo'
					}, {
						platforms: [6, 8],
						prereleases: [6],
						version: '4.5.6',
						firmware_vendor: 'Bar'
					}
				]
			};
			const e***REMOVED***pected = [
				{
					version: '1.2.3',
					platform: 0,
					prerelease: false,
					firmware_vendor: 'Foo'
				}, {
					version: '1.2.3',
					platform: 6,
					prerelease: false,

					firmware_vendor: 'Foo'
				}, {
					version: '4.5.6',
					platform: 6,
					prerelease: true,
					firmware_vendor: 'Bar'
				}, {
					version: '4.5.6',
					platform: 8,
					prerelease: false,
					firmware_vendor: 'Bar'
				},
			];
			api.listBuildTargets = () => {
				return Promise.resolve({ body: response });
			};
			return e***REMOVED***pect(client.listBuildTargets()).to.eventually.eql(e***REMOVED***pected);
		});
	});

	describe('trackingIdentity', () => {
		it('delegates to api and unpacks the body', () => {
			api.trackingIdentity = ({ auth, full, conte***REMOVED***t }) => {
				return Promise.resolve({ body: { auth, full, conte***REMOVED***t } });
			};
			const conte***REMOVED***t = { abd:123 };
			const full = 456;
			return e***REMOVED***pect(client.trackingIdentity({ full, conte***REMOVED***t })).to.eventually.eql({ auth: client.auth, full, conte***REMOVED***t });
		});

		it('delegates to api with default parameters and unpacks the body', () => {
			api.trackingIdentity = ({ auth, full, conte***REMOVED***t }) => {
				return Promise.resolve({ body: { auth, full, conte***REMOVED***t } });
			};
			const conte***REMOVED***t = undefined;
			const full = false;
			return e***REMOVED***pect(client.trackingIdentity()).to.eventually.eql({ auth:client.auth, full, conte***REMOVED***t });
		});
	});
});
