import { sinon, e***REMOVED***pect } from './test-setup';
import http from 'http';
import { EventEmitter } from 'events';

import EventStream from '../src/EventStream';

describe('EventStream', () => {
	afterEach(() => {
		sinon.restore();
	});

	function makeRequest() {
		const fakeRequest = new EventEmitter();
		fakeRequest.end = sinon.spy();
		fakeRequest.setTimeout = sinon.spy();
		return fakeRequest;
	}

	function makeResponse(statusCode) {
		const fakeResponse = new EventEmitter();
		fakeResponse.statusCode = statusCode;
		return fakeResponse;
	}

	describe('constructor', () => {
		it('creates an EventStream objects', () => {
			const eventStream = new EventStream('uri', 'token');

			e***REMOVED***pect(eventStream).to.be.obj;
		});
	});

	describe('connect', () => {
		it('successfully connects to http', () => {
			const fakeRequest = makeRequest();
			sinon.stub(http, 'request').callsFake(() => {
				setImmediate(() => {
					const fakeResponse = makeResponse(200);
					fakeRequest.emit('response', fakeResponse);
				});

				return fakeRequest;
			});

			const eventStream = new EventStream('http://hostname:8080/path', 'token');

			return eventStream.connect().then(() => {
				e***REMOVED***pect(http.request).to.have.been.calledWith({
					hostname: 'hostname',
					protocol: 'http:',
					path: '/path?access_token=token',
					method: 'get',
					port: 8080,
					mode: 'prefer-streaming'
				});
			});
		});

		it('returns http errors on connect', () => {
			const fakeRequest = makeRequest();
			sinon.stub(http, 'request').callsFake(() => {
				setImmediate(() => {
					const fakeResponse = makeResponse(500);
					fakeRequest.emit('response', fakeResponse);
					setImmediate(() => {
						fakeResponse.emit('data', '{"error":"unknown"}');
						fakeResponse.emit('end');
					});
				});

				return fakeRequest;
			});

			const eventStream = new EventStream('http://hostname:8080/path', 'token');

			return eventStream.connect().then(() => {
				throw new Error('e***REMOVED***pected to throw error');
			}, (reason) => {
				e***REMOVED***pect(reason).to.eql({
					statusCode: 500,
					errorDescription: 'HTTP error 500 from http://hostname:8080/path',
					body: {
						error: 'unknown'
					}
				});
			});
		});
	});

	describe('parse', () => {
		let eventStream;
		beforeEach(() => {
			eventStream = new EventStream();
			sinon.stub(eventStream, 'parseEventStreamLine');
		});

		it('accumulates date into the buffer before parsing line', () => {
			eventStream.parse('wo');
			eventStream.parse('rd');

			e***REMOVED***pect(eventStream.buf).to.eql('word');
			e***REMOVED***pect(eventStream.parseEventStreamLine).not.to.have.been.called;
		});

		it('parses a line ending with \\n', () => {
			const line = 'field: value\n';

			eventStream.parse(line);

			e***REMOVED***pect(eventStream.parseEventStreamLine).to.have.been.calledWith(0, line.inde***REMOVED***Of(':'), line.inde***REMOVED***Of('\n'));
		});

		it('parses 2 lines ending with \\n', () => {
			const line1 = 'field: value\n';
			const line2 = 'field2: value2\n';

			eventStream.parse(line1 + line2);

			e***REMOVED***pect(eventStream.parseEventStreamLine).to.have.been.calledWith(0, line1.inde***REMOVED***Of(':'), line1.inde***REMOVED***Of('\n'));
			e***REMOVED***pect(eventStream.parseEventStreamLine).to.have.been.calledWith(line1.length, line2.inde***REMOVED***Of(':'), line2.inde***REMOVED***Of('\n'));
		});


		it('parses a line ending with \\r\\n', () => {
			const line = 'field: value\r\n';

			eventStream.parse(line);

			e***REMOVED***pect(eventStream.parseEventStreamLine).to.have.been.calledWith(0, line.inde***REMOVED***Of(':'), line.inde***REMOVED***Of('\r'));
		});

		it('parses 2 lines ending with \\r\\n', () => {
			const line1 = 'field: value\r\n';
			const line2 = 'field2: value2\r\n';

			eventStream.parse(line1 + line2);

			e***REMOVED***pect(eventStream.parseEventStreamLine).to.have.been.calledWith(0, line1.inde***REMOVED***Of(':'), line1.inde***REMOVED***Of('\r'));
			e***REMOVED***pect(eventStream.parseEventStreamLine).to.have.been.calledWith(line1.length, line2.inde***REMOVED***Of(':'), line2.inde***REMOVED***Of('\r'));
		});

		it('clears buffer after parsing full lines', () => {
			eventStream.parse('field: value\n');

			e***REMOVED***pect(eventStream.buf).to.eql('');
		});

		it('keeps partial lines in buffer after parsing full lines', () => {
			eventStream.parse('field: value\nfield2');

			e***REMOVED***pect(eventStream.buf).to.eql('field2');
		});
	});

	describe('parseEventStreamLine', () => {
		let eventStream;
		beforeEach(() => {
			eventStream = new EventStream();
		});

		it('ignores comments', () => {
			// comments starts with : at column 0
			const line = ':ok\n';
			eventStream.buf = line;

			eventStream.parseEventStreamLine(0, line.inde***REMOVED***Of(':'), line.inde***REMOVED***Of('\n'));

			e***REMOVED***pect(eventStream.event).not.to.be.ok;
			e***REMOVED***pect(eventStream.data).to.be.eql('');
		});

		it('saves event name', () => {
			const line = 'event: testevent\n';
			eventStream.buf = line;

			eventStream.parseEventStreamLine(0, line.inde***REMOVED***Of(':'), line.inde***REMOVED***Of('\n'));

			e***REMOVED***pect(eventStream.event).to.be.true;
			e***REMOVED***pect(eventStream.eventName).to.eql('testevent');
		});

		it('saves event data', () => {
			const line = 'data: {"data":"test"}\n';
			eventStream.buf = line;

			eventStream.parseEventStreamLine(0, line.inde***REMOVED***Of(':'), line.inde***REMOVED***Of('\n'));

			e***REMOVED***pect(eventStream.data).to.eql('{"data":"test"}\n');
		});

		it('saves event name and data on separate lines', () => {
			const lines = ['event: testevent\n', 'data: {"data":"test"}\n'];
			for (let line of lines) {
				eventStream.buf = line;

				eventStream.parseEventStreamLine(0, line.inde***REMOVED***Of(':'), line.inde***REMOVED***Of('\n'));
			}

			e***REMOVED***pect(eventStream.event).to.be.true;
			e***REMOVED***pect(eventStream.eventName).to.eql('testevent');
			e***REMOVED***pect(eventStream.data).to.eql('{"data":"test"}\n');
		});

		it('emits event on blank line after saving event name and data', () => {
			const handler = sinon.spy();
			eventStream.event = true;
			eventStream.eventName = 'testevent';
			eventStream.data = '{"data":"test"}\n';
			eventStream.on('event', handler);

			eventStream.parseEventStreamLine(0, -1, 0);

			e***REMOVED***pect(handler).to.have.been.calledWith({
				name: 'testevent',
				data: 'test'
			});
		});

		it('emits error if the event handler crashes', () => {
			const errorHandler = sinon.spy();
			eventStream.event = true;
			eventStream.eventName = 'testevent';
			eventStream.data = '{"data":"test"}\n';
			eventStream.on('error', errorHandler);
			eventStream.on('event', () => {
				throw new Error('failed!');
			});

			eventStream.parseEventStreamLine(0, -1, 0);

			e***REMOVED***pect(errorHandler).to.have.been.called;
		});

		it('clears the event after emitting it', () => {
			eventStream.event = true;
			eventStream.eventName = 'testevent';
			eventStream.data = '{"data":"test"}\n';

			eventStream.parseEventStreamLine(0, -1, 0);

			e***REMOVED***pect(eventStream.event).to.be.false;
			e***REMOVED***pect(eventStream.eventName).to.be.undefined;
			e***REMOVED***pect(eventStream.data).to.eql('');
		});

		it('ignores multiple blank lines in succession', () => {
			const handler = sinon.spy();
			eventStream.on('event', handler);

			eventStream.parseEventStreamLine(0, -1, 0);
			eventStream.parseEventStreamLine(0, -1, 0);
			eventStream.parseEventStreamLine(0, -1, 0);

			e***REMOVED***pect(handler).not.to.have.been.called;
		});
	});
});
