/**
 * This serves thre functions:
 *
 * 1. Take any "aliases" and e***REMOVED***plode them to refecence the concrete types
 * 2. Normalize all handlers to have an `{enter, e***REMOVED***it}` pair, rather than raw functions
 * 3. make the enter and e***REMOVED***it handlers arrays, so that multiple handlers can be merged
 */
e***REMOVED***port default function e***REMOVED***plode(input: any): any;
