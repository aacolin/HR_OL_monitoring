'use strict';

module.e***REMOVED***ports = (
// node 12+
	process.allowedNodeEnvironmentFlags && process.allowedNodeEnvironmentFlags.has('--preserve-symlinks')
) || (
// node v6.2 - v11
	String(module.constructor._findPath).inde***REMOVED***Of('preserveSymlinks') >= 0 // eslint-disable-line no-underscore-dangle
);
