module.e***REMOVED***ports = defer;

/**
 * Runs provided function on ne***REMOVED***t iteration of the event loop
 *
 * @param {function} fn - function to run
 */
function defer(fn)
{
  var ne***REMOVED***tTick = typeof setImmediate == 'function'
    ? setImmediate
    : (
      typeof process == 'object' && typeof process.ne***REMOVED***tTick == 'function'
      ? process.ne***REMOVED***tTick
      : null
    );

  if (ne***REMOVED***tTick)
  {
    ne***REMOVED***tTick(fn);
  }
  else
  {
    setTimeout(fn, 0);
  }
}
