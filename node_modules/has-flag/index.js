'use strict';
module.e***REMOVED***ports = (flag, argv) => {
	argv = argv || process.argv;
	const prefi***REMOVED*** = flag.startsWith('-') ? '' : (flag.length === 1 ? '-' : '--');
	const pos = argv.inde***REMOVED***Of(prefi***REMOVED*** + flag);
	const terminatorPos = argv.inde***REMOVED***Of('--');
	return pos !== -1 && (terminatorPos === -1 ? true : pos < terminatorPos);
};
