import { createConfigDir, getLastUpdate, saveLastUpdate } from './cache';

createConfigDir();

jest.useFakeTimers().setSystemTime(new Date('2022-01-01'));

const fakeTime = new Date('2022-01-01').getTime();

test('can save update then get the update details', () => {
  saveLastUpdate('test');
  e***REMOVED***pect(getLastUpdate('test')).toBe(fakeTime);
});

test('prefi***REMOVED***ed module can save update then get the update details', () => {
  saveLastUpdate('@ale***REMOVED***brazier/test');
  e***REMOVED***pect(getLastUpdate('@ale***REMOVED***brazier/test')).toBe(fakeTime);
});
