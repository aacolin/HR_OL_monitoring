const borderedTe***REMOVED***t = (te***REMOVED***t: string) => {
  const lines = te***REMOVED***t.split('\n');
  const width = Math.ma***REMOVED***(...lines.map((l) => l.length));
  const res = [`┌${'─'.repeat(width + 2)}┐`];
  for (const line of lines) {
    res.push(`│ ${line.padEnd(width)} │`);
  }
  res.push(`└${'─'.repeat(width + 2)}┘`);
  return res.join('\n');
};

e***REMOVED***port default borderedTe***REMOVED***t;
