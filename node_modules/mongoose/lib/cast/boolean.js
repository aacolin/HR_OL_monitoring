'use strict';

const CastError = require('../error/cast');

/**
 * Given a value, cast it to a boolean, or throw a `CastError` if the value
 * cannot be casted. `null` and `undefined` are considered valid.
 *
 * @param {Any} value
 * @param {String} [path] optional the path to set on the CastError
 * @return {Boolean|null|undefined}
 * @throws {CastError} if `value` is not one of the allowed values
 * @api private
 */

module.e***REMOVED***ports = function castBoolean(value, path) {
  if (module.e***REMOVED***ports.convertToTrue.has(value)) {
    return true;
  }
  if (module.e***REMOVED***ports.convertToFalse.has(value)) {
    return false;
  }

  if (value == null) {
    return value;
  }

  throw new CastError('boolean', value, path);
};

module.e***REMOVED***ports.convertToTrue = new Set([true, 'true', 1, '1', 'yes']);
module.e***REMOVED***ports.convertToFalse = new Set([false, 'false', 0, '0', 'no']);
