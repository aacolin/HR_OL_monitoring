'use strict';

const castBoolean = require('../../cast/boolean');

/*!
 * ignore
 */

module.e***REMOVED***ports = function(val) {
  const path = this != null ? this.path : null;
  return castBoolean(val, path);
};
