'use strict';

/**
 * Determines if `arg` is an object.
 *
 * @param {Object|Array|String|Function|RegE***REMOVED***p|any} arg
 * @api private
 * @return {Boolean}
 */

module.e***REMOVED***ports = function(arg) {
  return (
    Buffer.isBuffer(arg) ||
    Object.prototype.toString.call(arg) === '[object Object]'
  );
};
