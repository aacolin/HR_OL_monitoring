'use strict';

module.e***REMOVED***ports = function decorateDiscriminatorInde***REMOVED***Options(schema, inde***REMOVED***Options) {
  // If the model is a discriminator and has an inde***REMOVED***, add a
  // partialFilterE***REMOVED***pression by default so the inde***REMOVED*** will only apply
  // to that discriminator.
  const discriminatorName = schema.discriminatorMapping && schema.discriminatorMapping.value;
  if (discriminatorName && !('sparse' in inde***REMOVED***Options)) {
    const discriminatorKey = schema.options.discriminatorKey;
    inde***REMOVED***Options.partialFilterE***REMOVED***pression = inde***REMOVED***Options.partialFilterE***REMOVED***pression || {};
    inde***REMOVED***Options.partialFilterE***REMOVED***pression[discriminatorKey] = discriminatorName;
  }
  return inde***REMOVED***Options;
};
