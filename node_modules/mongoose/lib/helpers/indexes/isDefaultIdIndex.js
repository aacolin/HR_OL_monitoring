'use strict';

const get = require('../get');

module.e***REMOVED***ports = function isDefaultIdInde***REMOVED***(inde***REMOVED***) {
  if (Array.isArray(inde***REMOVED***)) {
    // Mongoose synta***REMOVED***
    const keys = Object.keys(inde***REMOVED***[0]);
    return keys.length === 1 && keys[0] === '_id' && inde***REMOVED***[0]._id !== 'hashed';
  }

  if (typeof inde***REMOVED*** !== 'object') {
    return false;
  }

  const key = get(inde***REMOVED***, 'key', {});
  return Object.keys(key).length === 1 && key.hasOwnProperty('_id');
};
