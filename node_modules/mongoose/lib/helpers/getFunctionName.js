'use strict';

const functionNameRE = /^function\s*([^\s(]+)/;

module.e***REMOVED***ports = function(fn) {
  return (
    fn.name ||
    (fn.toString().trim().match(functionNameRE) || [])[1]
  );
};
