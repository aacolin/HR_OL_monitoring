'use strict';

const specialKeys = new Set([
  '$ref',
  '$id',
  '$db'
]);

module.e***REMOVED***ports = function isOperator(path) {
  return (
    path[0] === '$' &&
    !specialKeys.has(path)
  );
};
