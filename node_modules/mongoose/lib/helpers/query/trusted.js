'use strict';

const trustedSymbol = Symbol('mongoose#trustedSymbol');

e***REMOVED***ports.trustedSymbol = trustedSymbol;

e***REMOVED***ports.trusted = function trusted(obj) {
  if (obj == null || typeof obj !== 'object') {
    return obj;
  }
  obj[trustedSymbol] = true;
  return obj;
};
