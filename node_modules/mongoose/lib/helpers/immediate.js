/*!
 * Centralize this so we can more easily work around issues with people
 * stubbing out `process.ne***REMOVED***tTick()` in tests using sinon:
 * https://github.com/sinonjs/lole***REMOVED***#automatically-incrementing-mocked-time
 * See gh-6074
 */

'use strict';

const ne***REMOVED***tTick = typeof process !== 'undefined' && typeof process.ne***REMOVED***tTick === 'function' ?
  process.ne***REMOVED***tTick.bind(process) :
  cb => setTimeout(cb, 0); // Fallback for browser build

module.e***REMOVED***ports = function immediate(cb) {
  return ne***REMOVED***tTick(cb);
};
