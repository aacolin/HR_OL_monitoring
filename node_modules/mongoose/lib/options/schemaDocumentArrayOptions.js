'use strict';

const SchemaTypeOptions = require('./schemaTypeOptions');

/**
 * The options defined on an Document Array schematype.
 *
 * #### E***REMOVED***ample:
 *
 *     const schema = new Schema({ users: [{ name: string }] });
 *     schema.path('users').options; // SchemaDocumentArrayOptions instance
 *
 * @api public
 * @inherits SchemaTypeOptions
 * @constructor SchemaDocumentOptions
 */

class SchemaDocumentArrayOptions e***REMOVED***tends SchemaTypeOptions {}

const opts = require('./propertyOptions');

/**
 * If `true`, Mongoose will skip building any inde***REMOVED***es defined in this array's schema.
 * If not set, Mongoose will build all inde***REMOVED***es defined in this array's schema.
 *
 * #### E***REMOVED***ample:
 *
 *     const childSchema = Schema({ name: { type: String, inde***REMOVED***: true } });
 *     // If `e***REMOVED***cludeInde***REMOVED***es` is `true`, Mongoose will skip building an inde***REMOVED***
 *     // on `arr.name`. Otherwise, Mongoose will build an inde***REMOVED*** on `arr.name`.
 *     const parentSchema = Schema({
 *       arr: { type: [childSchema], e***REMOVED***cludeInde***REMOVED***es: true }
 *     });
 *
 * @api public
 * @property e***REMOVED***cludeInde***REMOVED***es
 * @memberOf SchemaDocumentArrayOptions
 * @type {Array}
 * @instance
 */

Object.defineProperty(SchemaDocumentArrayOptions.prototype, 'e***REMOVED***cludeInde***REMOVED***es', opts);

/**
 * If set, overwrites the child schema's `_id` option.
 *
 * #### E***REMOVED***ample:
 *
 *     const childSchema = Schema({ name: String });
 *     const parentSchema = Schema({
 *       child: { type: childSchema, _id: false }
 *     });
 *     parentSchema.path('child').schema.options._id; // false
 *
 * @api public
 * @property _id
 * @memberOf SchemaDocumentArrayOptions
 * @type {Array}
 * @instance
 */

Object.defineProperty(SchemaDocumentArrayOptions.prototype, '_id', opts);

/*!
 * ignore
 */

module.e***REMOVED***ports = SchemaDocumentArrayOptions;
