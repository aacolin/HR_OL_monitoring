require('../../modules/es6.rege***REMOVED***p.replace');
var REPLACE = require('../../modules/_wks')('replace');
module.e***REMOVED***ports = function (it, str, replacer) {
  return RegE***REMOVED***p.prototype[REPLACE].call(it, str, replacer);
};
