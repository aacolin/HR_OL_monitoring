// 7.1.15 ToLength
var toInteger = require('./_to-integer');
var min = Math.min;
module.e***REMOVED***ports = function (it) {
  return it > 0 ? min(toInteger(it), 0***REMOVED***1fffffffffffff) : 0; // pow(2, 53) - 1 == 9007199254740991
};
