// 9.4.2.3 ArraySpeciesCreate(originalArray, length)
var speciesConstructor = require('./_array-species-constructor');

module.e***REMOVED***ports = function (original, length) {
  return new (speciesConstructor(original))(length);
};
