var dP = require('./_object-dp');
var gOPD = require('./_object-gopd');
var ownKeys = require('./_own-keys');
var toIObject = require('./_to-iobject');

module.e***REMOVED***ports = function define(target, mi***REMOVED***in) {
  var keys = ownKeys(toIObject(mi***REMOVED***in));
  var length = keys.length;
  var i = 0;
  var key;
  while (length > i) dP.f(target, key = keys[i++], gOPD.f(mi***REMOVED***in, key));
  return target;
};
