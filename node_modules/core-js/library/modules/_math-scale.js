// https://rwaldron.github.io/proposal-math-e***REMOVED***tensions/
module.e***REMOVED***ports = Math.scale || function scale(***REMOVED***, inLow, inHigh, outLow, outHigh) {
  if (
    arguments.length === 0
      // eslint-disable-ne***REMOVED***t-line no-self-compare
      || ***REMOVED*** != ***REMOVED***
      // eslint-disable-ne***REMOVED***t-line no-self-compare
      || inLow != inLow
      // eslint-disable-ne***REMOVED***t-line no-self-compare
      || inHigh != inHigh
      // eslint-disable-ne***REMOVED***t-line no-self-compare
      || outLow != outLow
      // eslint-disable-ne***REMOVED***t-line no-self-compare
      || outHigh != outHigh
  ) return NaN;
  if (***REMOVED*** === Infinity || ***REMOVED*** === -Infinity) return ***REMOVED***;
  return (***REMOVED*** - inLow) * (outHigh - outLow) / (inHigh - inLow) + outLow;
};
