// 7.2.8 IsRegE***REMOVED***p(argument)
var isObject = require('./_is-object');
var cof = require('./_cof');
var MATCH = require('./_wks')('match');
module.e***REMOVED***ports = function (it) {
  var isRegE***REMOVED***p;
  return isObject(it) && ((isRegE***REMOVED***p = it[MATCH]) !== undefined ? !!isRegE***REMOVED***p : cof(it) == 'RegE***REMOVED***p');
};
