// false -> Array#inde***REMOVED***Of
// true  -> Array#includes
var toIObject = require('./_to-iobject');
var toLength = require('./_to-length');
var toAbsoluteInde***REMOVED*** = require('./_to-absolute-inde***REMOVED***');
module.e***REMOVED***ports = function (IS_INCLUDES) {
  return function ($this, el, fromInde***REMOVED***) {
    var O = toIObject($this);
    var length = toLength(O.length);
    var inde***REMOVED*** = toAbsoluteInde***REMOVED***(fromInde***REMOVED***, length);
    var value;
    // Array#includes uses SameValueZero equality algorithm
    // eslint-disable-ne***REMOVED***t-line no-self-compare
    if (IS_INCLUDES && el != el) while (length > inde***REMOVED***) {
      value = O[inde***REMOVED***++];
      // eslint-disable-ne***REMOVED***t-line no-self-compare
      if (value != value) return true;
    // Array#inde***REMOVED***Of ignores holes, Array#includes - not
    } else for (;length > inde***REMOVED***; inde***REMOVED***++) if (IS_INCLUDES || inde***REMOVED*** in O) {
      if (O[inde***REMOVED***] === el) return IS_INCLUDES || inde***REMOVED*** || 0;
    } return !IS_INCLUDES && -1;
  };
};
