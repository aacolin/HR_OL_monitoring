'use strict';
var $e***REMOVED***port = require('./_e***REMOVED***port');
var createProperty = require('./_create-property');

// WebKit Array.of isn't generic
$e***REMOVED***port($e***REMOVED***port.S + $e***REMOVED***port.F * require('./_fails')(function () {
  function F() { /* empty */ }
  return !(Array.of.call(F) instanceof F);
}), 'Array', {
  // 22.1.2.3 Array.of( ...items)
  of: function of(/* ...args */) {
    var inde***REMOVED*** = 0;
    var aLen = arguments.length;
    var result = new (typeof this == 'function' ? this : Array)(aLen);
    while (aLen > inde***REMOVED***) createProperty(result, inde***REMOVED***, arguments[inde***REMOVED***++]);
    result.length = aLen;
    return result;
  }
});
