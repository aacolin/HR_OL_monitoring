// 21.1.3.6 String.prototype.endsWith(searchString [, endPosition])
'use strict';
var $e***REMOVED***port = require('./_e***REMOVED***port');
var toLength = require('./_to-length');
var conte***REMOVED***t = require('./_string-conte***REMOVED***t');
var ENDS_WITH = 'endsWith';
var $endsWith = ''[ENDS_WITH];

$e***REMOVED***port($e***REMOVED***port.P + $e***REMOVED***port.F * require('./_fails-is-rege***REMOVED***p')(ENDS_WITH), 'String', {
  endsWith: function endsWith(searchString /* , endPosition = @length */) {
    var that = conte***REMOVED***t(this, searchString, ENDS_WITH);
    var endPosition = arguments.length > 1 ? arguments[1] : undefined;
    var len = toLength(that.length);
    var end = endPosition === undefined ? len : Math.min(toLength(endPosition), len);
    var search = String(searchString);
    return $endsWith
      ? $endsWith.call(that, search, end)
      : that.slice(end - search.length, end) === search;
  }
});
