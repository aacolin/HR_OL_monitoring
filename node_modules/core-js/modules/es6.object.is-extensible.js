// 19.1.2.11 Object.isE***REMOVED***tensible(O)
var isObject = require('./_is-object');

require('./_object-sap')('isE***REMOVED***tensible', function ($isE***REMOVED***tensible) {
  return function isE***REMOVED***tensible(it) {
    return isObject(it) ? $isE***REMOVED***tensible ? $isE***REMOVED***tensible(it) : true : false;
  };
});
