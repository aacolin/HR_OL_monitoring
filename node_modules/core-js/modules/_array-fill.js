// 22.1.3.6 Array.prototype.fill(value, start = 0, end = this.length)
'use strict';
var toObject = require('./_to-object');
var toAbsoluteInde***REMOVED*** = require('./_to-absolute-inde***REMOVED***');
var toLength = require('./_to-length');
module.e***REMOVED***ports = function fill(value /* , start = 0, end = @length */) {
  var O = toObject(this);
  var length = toLength(O.length);
  var aLen = arguments.length;
  var inde***REMOVED*** = toAbsoluteInde***REMOVED***(aLen > 1 ? arguments[1] : undefined, length);
  var end = aLen > 2 ? arguments[2] : undefined;
  var endPos = end === undefined ? length : toAbsoluteInde***REMOVED***(end, length);
  while (endPos > inde***REMOVED***) O[inde***REMOVED***++] = value;
  return O;
};
