require('../../modules/es6.rege***REMOVED***p.match');
var MATCH = require('../../modules/_wks')('match');
module.e***REMOVED***ports = function (it, str) {
  return RegE***REMOVED***p.prototype[MATCH].call(it, str);
};
