require('../../modules/es6.rege***REMOVED***p.split');
var SPLIT = require('../../modules/_wks')('split');
module.e***REMOVED***ports = function (it, str, limit) {
  return RegE***REMOVED***p.prototype[SPLIT].call(it, str, limit);
};
