/**
 * Continues with the callback on the ne***REMOVED***t tick.
 * @function
 * @param {function(...[*])} callback Callback to e***REMOVED***ecute
 * @inner
 */
var ne***REMOVED***tTick = typeof process !== 'undefined' && process && typeof process.ne***REMOVED***tTick === 'function'
    ? (typeof setImmediate === 'function' ? setImmediate : process.ne***REMOVED***tTick)
    : setTimeout;

/**
 * Converts a JavaScript string to UTF8 bytes.
 * @param {string} str String
 * @returns {!Array.<number>} UTF8 bytes
 * @inner
 */
function stringToBytes(str) {
    var out = [],
        i = 0;
    utf***REMOVED***.encodeUTF16toUTF8(function() {
        if (i >= str.length) return null;
        return str.charCodeAt(i++);
    }, function(b) {
        out.push(b);
    });
    return out;
}

//? include("util/base64.js");

//? include("../../node_modules/utf***REMOVED***/dist/utf***REMOVED***-embeddable.js");

Date.now = Date.now || function() { return +new Date; }; 
