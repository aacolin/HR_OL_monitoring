'use strict';

var inspect = require('../');
var test = require('tape');
var hasToStringTag = require('has-tostringtag/shams')();
var forEach = require('for-each');

test('fakes', { skip: !hasToStringTag }, function (t) {
    forEach([
        'Array',
        'Boolean',
        'Date',
        'Error',
        'Number',
        'RegE***REMOVED***p',
        'String'
    ], function (e***REMOVED***pected) {
        var faker = {};
        faker[Symbol.toStringTag] = e***REMOVED***pected;

        t.equal(
            inspect(faker),
            '{ [Symbol(Symbol.toStringTag)]: \'' + e***REMOVED***pected + '\' }',
            'faker masquerading as ' + e***REMOVED***pected + ' is not shown as one'
        );
    });

    t.end();
});
