import { BSONValue } from './bson_value';
import { BSON_INT32_MAX, BSON_INT32_MIN } from './constants';
import { BSONError } from './error';
import type { EJSONOptions } from './e***REMOVED***tended_json';
import { type InspectFn, defaultInspect } from './parser/utils';
import { removeLeadingZerosAndE***REMOVED***plicitPlus } from './utils/string_utils';

/** @public */
e***REMOVED***port interface Int32E***REMOVED***tended {
  $numberInt: string;
}

/**
 * A class representation of a BSON Int32 type.
 * @public
 * @category BSONType
 */
e***REMOVED***port class Int32 e***REMOVED***tends BSONValue {
  get _bsontype(): 'Int32' {
    return 'Int32';
  }

  value!: number;
  /**
   * Create an Int32 type
   *
   * @param value - the number we want to represent as an int32.
   */
  constructor(value: number | string) {
    super();
    if ((value as unknown) instanceof Number) {
      value = value.valueOf();
    }

    this.value = +value | 0;
  }

  /**
   * Attempt to create an Int32 type from string.
   *
   * This method will throw a BSONError on any string input that is not representable as an Int32.
   * Notably, this method will also throw on the following string formats:
   * - Strings in non-decimal formats (e***REMOVED***ponent notation, binary, he***REMOVED***, or octal digits)
   * - Strings non-numeric and non-leading sign characters (e***REMOVED***: '2.0', '24,000')
   * - Strings with leading and/or trailing whitespace
   *
   * Strings with leading zeros, however, are allowed.
   *
   * @param value - the string we want to represent as an int32.
   */
  static fromString(value: string): Int32 {
    const cleanedValue = removeLeadingZerosAndE***REMOVED***plicitPlus(value);

    const coercedValue = Number(value);

    if (BSON_INT32_MAX < coercedValue) {
      throw new BSONError(`Input: '${value}' is larger than the ma***REMOVED***imum value for Int32`);
    } else if (BSON_INT32_MIN > coercedValue) {
      throw new BSONError(`Input: '${value}' is smaller than the minimum value for Int32`);
    } else if (!Number.isSafeInteger(coercedValue)) {
      throw new BSONError(`Input: '${value}' is not a safe integer`);
    } else if (coercedValue.toString() !== cleanedValue) {
      // catch all case
      throw new BSONError(`Input: '${value}' is not a valid Int32 string`);
    }
    return new Int32(coercedValue);
  }

  /**
   * Access the number value.
   *
   * @returns returns the wrapped int32 number.
   */
  valueOf(): number {
    return this.value;
  }

  toString(radi***REMOVED***?: number): string {
    return this.value.toString(radi***REMOVED***);
  }

  toJSON(): number {
    return this.value;
  }

  /** @internal */
  toE***REMOVED***tendedJSON(options?: EJSONOptions): number | Int32E***REMOVED***tended {
    if (options && (options.rela***REMOVED***ed || options.legacy)) return this.value;
    return { $numberInt: this.value.toString() };
  }

  /** @internal */
  static fromE***REMOVED***tendedJSON(doc: Int32E***REMOVED***tended, options?: EJSONOptions): number | Int32 {
    return options && options.rela***REMOVED***ed ? parseInt(doc.$numberInt, 10) : new Int32(doc.$numberInt);
  }

  inspect(depth?: number, options?: unknown, inspect?: InspectFn): string {
    inspect ??= defaultInspect;
    return `new Int32(${inspect(this.value, options)})`;
  }
}
