/*!
 * e***REMOVED***press
 * Copyright(c) 2009-2013 TJ Holowaychuk
 * Copyright(c) 2013 Roman Shtylman
 * Copyright(c) 2014-2015 Douglas Christopher Wilson
 * MIT Licensed
 */

'use strict';

/**
 * Module dependencies.
 * @private
 */

var debug = require('debug')('e***REMOVED***press:view');
var path = require('path');
var fs = require('fs');

/**
 * Module variables.
 * @private
 */

var dirname = path.dirname;
var basename = path.basename;
var e***REMOVED***tname = path.e***REMOVED***tname;
var join = path.join;
var resolve = path.resolve;

/**
 * Module e***REMOVED***ports.
 * @public
 */

module.e***REMOVED***ports = View;

/**
 * Initialize a new `View` with the given `name`.
 *
 * Options:
 *
 *   - `defaultEngine` the default template engine name
 *   - `engines` template engine require() cache
 *   - `root` root path for view lookup
 *
 * @param {string} name
 * @param {object} options
 * @public
 */

function View(name, options) {
  var opts = options || {};

  this.defaultEngine = opts.defaultEngine;
  this.e***REMOVED***t = e***REMOVED***tname(name);
  this.name = name;
  this.root = opts.root;

  if (!this.e***REMOVED***t && !this.defaultEngine) {
    throw new Error('No default engine was specified and no e***REMOVED***tension was provided.');
  }

  var fileName = name;

  if (!this.e***REMOVED***t) {
    // get e***REMOVED***tension from default engine name
    this.e***REMOVED***t = this.defaultEngine[0] !== '.'
      ? '.' + this.defaultEngine
      : this.defaultEngine;

    fileName += this.e***REMOVED***t;
  }

  if (!opts.engines[this.e***REMOVED***t]) {
    // load engine
    var mod = this.e***REMOVED***t.slice(1)
    debug('require "%s"', mod)

    // default engine e***REMOVED***port
    var fn = require(mod).__e***REMOVED***press

    if (typeof fn !== 'function') {
      throw new Error('Module "' + mod + '" does not provide a view engine.')
    }

    opts.engines[this.e***REMOVED***t] = fn
  }

  // store loaded engine
  this.engine = opts.engines[this.e***REMOVED***t];

  // lookup path
  this.path = this.lookup(fileName);
}

/**
 * Lookup view by the given `name`
 *
 * @param {string} name
 * @private
 */

View.prototype.lookup = function lookup(name) {
  var path;
  var roots = [].concat(this.root);

  debug('lookup "%s"', name);

  for (var i = 0; i < roots.length && !path; i++) {
    var root = roots[i];

    // resolve the path
    var loc = resolve(root, name);
    var dir = dirname(loc);
    var file = basename(loc);

    // resolve the file
    path = this.resolve(dir, file);
  }

  return path;
};

/**
 * Render with the given options.
 *
 * @param {object} options
 * @param {function} callback
 * @private
 */

View.prototype.render = function render(options, callback) {
  debug('render "%s"', this.path);
  this.engine(this.path, options, callback);
};

/**
 * Resolve the file within the given directory.
 *
 * @param {string} dir
 * @param {string} file
 * @private
 */

View.prototype.resolve = function resolve(dir, file) {
  var e***REMOVED***t = this.e***REMOVED***t;

  // <path>.<e***REMOVED***t>
  var path = join(dir, file);
  var stat = tryStat(path);

  if (stat && stat.isFile()) {
    return path;
  }

  // <path>/inde***REMOVED***.<e***REMOVED***t>
  path = join(dir, basename(file, e***REMOVED***t), 'inde***REMOVED***' + e***REMOVED***t);
  stat = tryStat(path);

  if (stat && stat.isFile()) {
    return path;
  }
};

/**
 * Return a stat, maybe.
 *
 * @param {string} path
 * @return {fs.Stats}
 * @private
 */

function tryStat(path) {
  debug('stat "%s"', path);

  try {
    return fs.statSync(path);
  } catch (e) {
    return undefined;
  }
}
