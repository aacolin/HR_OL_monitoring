"use strict";

e***REMOVED***ports.__esModule = true;

e***REMOVED***ports.default = function (val, name, undef) {
  if (val === undef) {
    throw new ReferenceError(name + " is not defined - temporal dead zone");
  } else {
    return val;
  }
};