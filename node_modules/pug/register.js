var pug = require('./');
var resolvedPug = JSON.stringify(require.resolve('./'));

function compileTemplate(module, filename) {
  var template = pug.compileFileClient(filename, {
    inlineRuntimeFunctions: false,
  });
  var body =
    'var pug = require(' +
    resolvedPug +
    ').runtime;\n\n' +
    'module.e***REMOVED***ports = ' +
    template +
    ';';
  module._compile(body, filename);
}

if (require.e***REMOVED***tensions) {
  require.e***REMOVED***tensions['.pug'] = compileTemplate;
}
