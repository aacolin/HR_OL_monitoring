module.e***REMOVED***ports = function (prefi***REMOVED***) {
    return function (request) {
        if (request.url[0] === '/') {
            request.url = prefi***REMOVED*** + request.url;
        }

        return request;
    };
}